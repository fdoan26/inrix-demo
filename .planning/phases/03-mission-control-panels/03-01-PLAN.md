---
phase: 03-mission-control-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mission-control/MissionControlView.tsx
  - src/components/panels/SegmentPanel.tsx
autonomous: false
requirements:
  - MAP-05
  - MAP-06
  - PANEL-01
  - PANEL-02
  - PANEL-03

must_haves:
  truths:
    - "Clicking any road segment polyline slides in a right panel showing segment name, ID, type, FRC, length, speed bucket, travel time (minutes), avg speed, free flow speed, historic avg speed"
    - "Clicking any camera pin slides in a right panel showing camera name, highway, direction, type, and camera image placeholder"
    - "Clicking the X close button on either panel causes it to disappear and the map expands back to full width"
    - "Travel time values display as minutes (not raw seconds) with realistic values (under 60 min for all segments)"
  artifacts:
    - path: "src/components/mission-control/MissionControlView.tsx"
      provides: "Panel host logic — reads selectedItem from Zustand, resolves full object from data files, conditionally renders SegmentPanel or CameraPanel"
      contains: "selectedItem"
    - path: "src/components/panels/SegmentPanel.tsx"
      provides: "Segment detail panel with corrected travel time display (seconds divided by 60)"
      contains: "Math.round"
  key_links:
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/store/uiSlice.ts"
      via: "useStore((s) => s.selectedItem)"
      pattern: "useStore.*selectedItem"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/components/panels/SegmentPanel.tsx"
      via: "conditional render when selectedItem.type === 'segment'"
      pattern: "selectedSegment.*SegmentPanel"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/components/panels/CameraPanel.tsx"
      via: "conditional render when selectedItem.type === 'camera'"
      pattern: "selectedCamera.*CameraPanel"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/data/segments.ts"
      via: "static import for segment lookup by ID"
      pattern: "import.*segments.*from.*data/segments"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/data/cameras.ts"
      via: "static import for camera lookup by ID"
      pattern: "import.*cameras.*from.*data/cameras"
---

<objective>
Wire the MissionControlView component to read the Zustand selectedItem state and conditionally render SegmentPanel or CameraPanel as a flex sibling beside the map. Fix the travelTime seconds-to-minutes display bug in SegmentPanel.

Purpose: This is the only missing piece for Phase 3 — all panel components, click handlers, store actions, and CSS animations already exist. The panels just need a host to render them.
Output: Clicking map elements opens detail panels; clicking close dismisses them.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mission-control-panels/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/store/uiSlice.ts:
```typescript
export type SelectedItem =
  | { type: 'segment'; id: string }
  | { type: 'camera'; id: string }
  | { type: 'alert'; id: string }
  | null

export interface UISlice {
  selectedItem: SelectedItem
  setSelectedItem: (item: SelectedItem) => void
  clearSelectedItem: () => void
}
```

From src/components/panels/SegmentPanel.tsx:
```typescript
interface Props {
  segment: Segment;
}
export const SegmentPanel: React.FC<Props>
```

From src/components/panels/CameraPanel.tsx:
```typescript
interface Props {
  camera: Camera;
}
export const CameraPanel: React.FC<Props>
```

From src/data/segments.ts:
```typescript
// Array of Segment objects; each has segmentId: string
export const segments: Segment[]
```

From src/data/cameras.ts:
```typescript
// Array of Camera objects; each has id: string
export const cameras: Camera[]
```

Current MissionControlView.tsx (the file to modify):
```typescript
import { FilterBar } from './FilterBar'
import { MapView } from './MapView'

export function MissionControlView() {
  return (
    <div className="flex-1 flex flex-col overflow-hidden" style={{ background: '#0a1628' }}>
      <FilterBar />
      <MapView />
    </div>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MissionControlView panel host and fix SegmentPanel travel time display</name>
  <files>src/components/mission-control/MissionControlView.tsx, src/components/panels/SegmentPanel.tsx</files>
  <read_first>
    src/components/mission-control/MissionControlView.tsx
    src/components/panels/SegmentPanel.tsx
    src/components/panels/CameraPanel.tsx
    src/store/uiSlice.ts
    src/data/segments.ts
    src/data/cameras.ts
  </read_first>
  <action>
**MissionControlView.tsx — replace entire file with panel host logic:**

1. Add imports at top:
   - `import { useStore } from '../../store'`
   - `import { segments } from '../../data/segments'`
   - `import { cameras } from '../../data/cameras'`
   - `import { SegmentPanel } from '../panels/SegmentPanel'`
   - `import { CameraPanel } from '../panels/CameraPanel'`
   - Keep existing `FilterBar` and `MapView` imports

2. Inside the component function, before the return:
   - `const selectedItem = useStore((s) => s.selectedItem)`
   - Derive `selectedSegment`: if `selectedItem?.type === 'segment'`, find from `segments` array using `segments.find((s) => s.segmentId === selectedItem.id) ?? null`, else `null`
   - Derive `selectedCamera`: if `selectedItem?.type === 'camera'`, find from `cameras` array using `cameras.find((c) => c.id === selectedItem.id) ?? null`, else `null`

3. Modify the JSX return:
   - Keep the outer div unchanged: `<div className="flex-1 flex flex-col overflow-hidden" style={{ background: '#0a1628' }}>`
   - Keep `<FilterBar />` as first child
   - Wrap `<MapView />` and panels in a flex row div: `<div style={{ display: 'flex', flex: 1, overflow: 'hidden' }}>`
   - Inside that flex row: `<MapView />` first (it already has `flex: 1` or equivalent styling to fill available space), then conditionally render `{selectedSegment && <SegmentPanel segment={selectedSegment} />}` and `{selectedCamera && <CameraPanel camera={selectedCamera} />}`

Use inline styles for the flex row wrapper (consistent with Phase 2 pattern — inline styles for layout around map components).

**SegmentPanel.tsx — fix travelTime seconds-to-minutes bug:**

4. Find the StatCard for "Current Travel Time" (around line 146). Change:
   - `value` from `` `${segment.travelTime} min` `` to `` `${Math.round(segment.travelTime / 60)} min` ``
   - `subValue` from `` `Avg: ${segment.avgTravelTime} min` `` to `` `Avg: ${Math.round(segment.avgTravelTime / 60)} min` ``

5. Find the delay indicator section (around line 227). Change:
   - `` +{Math.round((segment.travelTime - segment.avgTravelTime))} min `` to `` +{Math.round((segment.travelTime - segment.avgTravelTime) / 60)} min ``

Do NOT modify: CameraPanel.tsx (already correct), uiSlice.ts (already correct), SegmentLayer/CameraLayer click handlers (already call setSelectedItem), index.css animation (already defined).
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MissionControlView.tsx contains `import { useStore } from '../../store'`
    - MissionControlView.tsx contains `import { segments } from '../../data/segments'`
    - MissionControlView.tsx contains `import { cameras } from '../../data/cameras'`
    - MissionControlView.tsx contains `import { SegmentPanel } from '../panels/SegmentPanel'`
    - MissionControlView.tsx contains `import { CameraPanel } from '../panels/CameraPanel'`
    - MissionControlView.tsx contains `useStore((s) => s.selectedItem)`
    - MissionControlView.tsx contains `selectedItem?.type === 'segment'`
    - MissionControlView.tsx contains `selectedItem?.type === 'camera'`
    - MissionControlView.tsx contains `segments.find`
    - MissionControlView.tsx contains `cameras.find`
    - MissionControlView.tsx contains `display: 'flex', flex: 1, overflow: 'hidden'` (flex row wrapper)
    - MissionControlView.tsx contains `selectedSegment && <SegmentPanel`
    - MissionControlView.tsx contains `selectedCamera && <CameraPanel`
    - SegmentPanel.tsx contains `Math.round(segment.travelTime / 60)` (travel time fix)
    - SegmentPanel.tsx contains `Math.round(segment.avgTravelTime / 60)` (avg travel time fix)
    - SegmentPanel.tsx contains `Math.round((segment.travelTime - segment.avgTravelTime) / 60)` (delay fix)
    - TypeScript compiles with no errors (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>MissionControlView conditionally renders SegmentPanel or CameraPanel as a flex sibling beside MapView based on Zustand selectedItem state. SegmentPanel displays travel times in minutes (dividing raw seconds by 60). TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify panel interactions in browser</name>
  <files>src/components/mission-control/MissionControlView.tsx, src/components/panels/SegmentPanel.tsx</files>
  <action>Run `npm run dev` and open the app in browser for human verification of panel interactions.</action>
  <what-built>Detail panels for road segments and cameras wired into MissionControlView with slide-in animation, close button, and corrected travel time display.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173 in browser
    2. Click any colored road segment polyline on the map
       - Expected: A 320px panel slides in from the right showing segment name, congestion badge, travel time in minutes (should be under 60 min, NOT a number like 1680), current speed, free flow speed, historic avg speed, speed breakdown bars, delay indicator, and segment ID
       - Expected: The map narrows to accommodate the panel (flex layout, not overlay)
    3. Click the X button in the panel header
       - Expected: The panel disappears and the map expands back to full width
    4. Click any black camera pin on the map
       - Expected: A 320px panel slides in showing camera name, highway, direction, type badge, camera feed placeholder (dark box with scanlines, corner brackets, REC indicator), camera controls, and camera details table
    5. Click the X button to close the camera panel
       - Expected: Panel disappears, map expands
    6. Click a segment, then click a different camera (without closing first)
       - Expected: The segment panel is replaced by the camera panel
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npm run dev -- --host 2>&1 | head -5</automated>
  </verify>
  <done>User confirms all 6 verification steps pass: segment panel opens with correct travel times, camera panel opens with all attributes, close button works, panels replace each other on switching selection.</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles with `npx tsc --noEmit` (no errors)
- `npm run dev` serves the app without console errors
- Clicking a segment polyline opens SegmentPanel with correct minute-based travel times
- Clicking a camera pin opens CameraPanel with all attributes
- Close button dismisses both panel types
- Panel renders as flex sibling (map narrows, not overlapped)
</verification>

<success_criteria>
- All 5 requirements (MAP-05, MAP-06, PANEL-01, PANEL-02, PANEL-03) are satisfied
- Segment panel shows all required fields: name, segment ID, type, FRC level, length, speed bucket, avg travel time, avg speed, free flow speed, historic avg speed
- Camera panel shows all required fields: name, highway, direction, type, camera image placeholder
- Travel time displays as minutes (values under 60), not raw seconds (values like 1680)
- Close button works on both panels
- Panel slide-in animation plays on open
</success_criteria>

<output>
After completion, create `.planning/phases/03-mission-control-panels/03-01-SUMMARY.md`
</output>
