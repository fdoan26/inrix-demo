---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tailwind.config.js
  - postcss.config.js
  - src/main.tsx
  - src/index.css
  - src/types/index.ts
  - tsconfig.app.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "npm run build completes with zero errors after package reinstall"
    - "Tailwind v3 design token classes (bg-navy-deep, bg-navy-nav, text-text-muted, bg-congestion-green, etc.) generate valid CSS"
    - "Leaflet CSS is the first import in main.tsx before Tailwind globals"
    - "TypeScript interfaces for Segment, Alert, Camera use [number, number] tuples not {lat, lng} objects"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency versions"
      contains: "react.*18.3.1"
    - path: "tailwind.config.js"
      provides: "INRIX design tokens and z-index scale"
      contains: "navy-deep"
    - path: "postcss.config.js"
      provides: "Tailwind v3 PostCSS pipeline"
      contains: "tailwindcss"
    - path: "src/types/index.ts"
      provides: "All TypeScript interfaces for the project"
      contains: "positions: [number, number][]"
  key_links:
    - from: "src/main.tsx"
      to: "leaflet/dist/leaflet.css"
      via: "first import line"
      pattern: "import 'leaflet/dist/leaflet.css'"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@tailwind directives"
      pattern: "@tailwind base"
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react"
      via: "plugins array"
      pattern: "plugins.*react"
---

<objective>
Reinstall all project dependencies at the correct pinned versions, replace Tailwind v4 config with v3 PostCSS setup, rewrite config files, fix main.tsx import order, and define canonical TypeScript interfaces.

Purpose: The existing project has wrong package versions (React 19, react-leaflet v5, Tailwind v4, Vite 8, Recharts v3) that are incompatible with the locked CONTEXT.md requirements. This plan fixes the foundation so all subsequent plans can build on a working compile chain.

Output: A project that runs `npm run build` with zero errors, Tailwind v3 with design tokens, correct import order, and canonical type definitions.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/ROADMAP.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-CONTEXT.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-RESEARCH.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/research/STACK.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reinstall all packages with pinned versions</name>
  <files>package.json</files>
  <read_first>
    C:/Users/FisherDoan/Workspaces/inrix-demo/package.json
    C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
The current package.json has wrong versions: react@^19.2.4, react-dom@^19.2.4, react-leaflet@^5.0.0, recharts@^3.8.0, tailwindcss@^4.2.1, vite@^8.0.0, @vitejs/plugin-react@^6.0.0, @types/react@^19.2.14, @types/react-dom@^19.2.3. It also has @tailwindcss/vite (v4 plugin) and react-is (unnecessary).

Run the following commands sequentially from the project root (C:/Users/FisherDoan/Workspaces/inrix-demo):

1. Remove incompatible packages:
   npm uninstall @tailwindcss/vite react-is

2. Install correct production dependencies (overwriting wrong versions):
   npm install react@18.3.1 react-dom@18.3.1 leaflet@1.9.4 react-leaflet@4.2.1 recharts@2.15.4 zustand@5.0.12 lucide-react@0.577.0

3. Install correct dev dependencies:
   npm install -D vite@5.4.21 @vitejs/plugin-react@4.7.0 tailwindcss@3.4.19 postcss@8.5.8 autoprefixer@10.4.27 @types/leaflet@1.9.21 @types/react@18.3.28 @types/react-dom@18.3.1

4. Move @types/leaflet from dependencies to devDependencies if it ended up in dependencies:
   Check package.json — @types/leaflet should be in devDependencies only.

After all installs, verify package.json shows the correct pinned versions. The caret (^) ranges are acceptable as long as the resolved version in node_modules matches the target. Run `npm ls react` and `npm ls react-leaflet` to confirm resolved versions.

CRITICAL: Do NOT run `npm install` without version specifiers — npm defaults will install wrong versions.
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && node -e "const p=require('./package.json'); const checks=[['react','18'],['react-dom','18'],['react-leaflet','4'],['recharts','2'],['tailwindcss','3'],['vite','5']]; let ok=true; checks.forEach(([k,v])=>{ const ver=(p.dependencies[k]||p.devDependencies[k]||''); if(!ver.includes(v)){console.error(k+' wrong: '+ver); ok=false;}}); if(!ok) process.exit(1); console.log('All versions correct')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies.react contains "18.3.1"
    - package.json dependencies["react-leaflet"] contains "4.2.1"
    - package.json dependencies.recharts contains "2.15.4"
    - package.json devDependencies.tailwindcss contains "3.4.19"
    - package.json devDependencies.vite contains "5.4.21"
    - package.json devDependencies["@vitejs/plugin-react"] contains "4.7.0"
    - package.json does NOT contain "@tailwindcss/vite"
    - package.json does NOT contain "react-is"
    - @types/react contains "18.3" (not 19.x)
    - @types/react-dom contains "18.3" (not 19.x)
  </acceptance_criteria>
  <done>All dependencies installed at pinned versions matching STACK.md. @tailwindcss/vite and react-is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite config files, main.tsx, index.css, and types</name>
  <files>
    vite.config.ts
    tailwind.config.js
    postcss.config.js
    src/main.tsx
    src/index.css
    src/types/index.ts
    tsconfig.app.json
  </files>
  <read_first>
    C:/Users/FisherDoan/Workspaces/inrix-demo/vite.config.ts
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/main.tsx
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/index.css
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/types/index.ts
    C:/Users/FisherDoan/Workspaces/inrix-demo/tsconfig.app.json
    C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Rewrite the following files. Each file has specific content requirements:

**vite.config.ts** — Remove @tailwindcss/vite import and plugin. Keep only react plugin:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

**tailwind.config.js** — Create NEW file (does not exist). Use ES module export. Must include ALL INRIX design tokens and extended z-index:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        'navy-deep': '#0a1628',
        'navy-nav': '#0d1f3c',
        'accent-blue': '#1a6eb5',
        'toggle-blue': '#2196f3',
        'alert-red': '#e53935',
        'alert-orange': '#ff9800',
        'alert-yellow': '#ffeb3b',
        'alert-blue': '#2196f3',
        'text-muted': '#8ca0bc',
        'border-dark': '#1e3a5f',
        'congestion-green': '#4caf50',
        'congestion-yellow': '#ffeb3b',
        'congestion-orange': '#ff9800',
        'congestion-red': '#f44336',
        'los-a': '#1b5e20',
        'los-b': '#4caf50',
        'los-c': '#8bc34a',
        'los-d': '#ff9800',
        'los-e': '#f44336',
        'los-f': '#b71c1c',
      },
      zIndex: {
        '100': '100',
        '500': '500',
        '900': '900',
        '1000': '1000',
      },
    },
  },
  plugins: [],
}
```

**postcss.config.js** — Create NEW file:
```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

**src/main.tsx** — Rewrite with Leaflet CSS as FIRST import:
```typescript
import 'leaflet/dist/leaflet.css'
import './index.css'
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**src/index.css** — Replace `@import "tailwindcss"` (v4) with v3 directives. KEEP all the Leaflet overrides, scrollbar styles, toggle-switch styles, panel animation, and recharts styles from the existing file. Only change the first line:
Replace `@import "tailwindcss";` with:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```
Keep the `@layer base` block (html/body/root styles), and ALL existing CSS rules below it unchanged.

**src/types/index.ts** — Replace entirely with canonical interfaces from CONTEXT.md. The key changes from existing types:
- Remove `Module` type (was 5 modules, now only 2 views)
- Remove `LatLng` interface (use tuples)
- `Segment.coords` becomes `Segment.positions: [number, number][]`
- Add `Segment.type: string`, `Segment.frc: number`, `Segment.lengthMiles: number`, `Segment.speedBucket: number`
- `Segment.travelTime` and `Segment.avgTravelTime` in SECONDS (not minutes)
- `Alert.position: [number, number]` (tuple not object)
- `Alert.severity: 'low' | 'medium' | 'high'` (not 'moderate'/'severe')
- `Camera.position: [number, number]` (tuple not object)
- Add `Camera.highway: string`, `Camera.clusterCount: number`
- Remove `FilterState`, `PanelContent` (moved to store slices)
- Add `ActiveView` type: `'mission-control' | 'signal-analytics'`
- Keep and update signal analytics types to match CONTEXT.md exact values

Full types/index.ts content:
```typescript
export type ActiveView = 'mission-control' | 'signal-analytics'

export interface Segment {
  segmentId: string
  name: string
  positions: [number, number][]
  currentSpeed: number
  freeFlowSpeed: number
  historicAvgSpeed: number
  congestionLevel: number
  travelTime: number
  avgTravelTime: number
  type: string
  frc: number
  lengthMiles: number
  speedBucket: number
  highway: string
  direction: 'NB' | 'SB' | 'EB' | 'WB'
}

export type AlertType =
  | 'crash'
  | 'construction'
  | 'dangerous_slowdown'
  | 'road_closure'
  | 'event'
  | 'hazard'
  | 'congestion'

export interface Alert {
  id: string
  type: AlertType
  position: [number, number]
  description: string
  severity: 'low' | 'medium' | 'high'
  timestamp: string
  roadName: string
}

export interface Camera {
  id: string
  name: string
  position: [number, number]
  highway: string
  direction: string
  type: string
  clusterCount: number
}

export interface SignalKPIs {
  intersections: number
  approaches: number
  cameras: number
  corridors: number
}

export interface ControlDelayTotals {
  total: number
  fourWeekAvg: number
  delta: string
}

export interface WeeklyChartPoint {
  week: string
  value: number
}

export interface LOSGrade {
  grade: 'A' | 'B' | 'C' | 'D' | 'E' | 'F'
  count: number
  color: string
}

export interface DelayIssueRow {
  name: string
  avg4wk: string
  current: string
  delta: string
}

export interface CorridorIssueRow {
  name: string
  avg4wk: string
  current: string
  delta: string
  pct: string
}

export interface SignalData {
  kpis: SignalKPIs
  controlDelayTotals: ControlDelayTotals
  avgControlDelayPerVehicle: ControlDelayTotals
  weeklyChart: WeeklyChartPoint[]
  losByGrade: LOSGrade[]
  topWorsenedTotal: DelayIssueRow[]
  topWorsenedPerVehicle: DelayIssueRow[]
  corridorsTravelTime: CorridorIssueRow[]
  corridorsTTI: CorridorIssueRow[]
}
```

**tsconfig.app.json** — Read the current file first. Ensure these settings:
- `moduleResolution: "bundler"` (NOT "NodeNext" — breaks react-leaflet v4)
- `skipLibCheck: true` (required for recharts v2 + @types/react@18 compat)
- `jsx: "react-jsx"`
- `strict: true`
If the existing file already has these, leave it unchanged. If it uses NodeNext, change to bundler.
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts does NOT contain "@tailwindcss/vite"
    - vite.config.ts contains "import react from '@vitejs/plugin-react'"
    - tailwind.config.js exists and contains "navy-deep": "#0a1628"
    - tailwind.config.js contains "'1000': '1000'"
    - postcss.config.js exists and contains "tailwindcss: {}"
    - src/main.tsx first non-empty line is: import 'leaflet/dist/leaflet.css'
    - src/main.tsx second import is: import './index.css'
    - src/index.css contains "@tailwind base;" (NOT "@import 'tailwindcss'")
    - src/index.css still contains ".leaflet-container" (Leaflet overrides preserved)
    - src/types/index.ts contains "positions: [number, number][]"
    - src/types/index.ts contains "export type ActiveView"
    - src/types/index.ts contains "frc: number"
    - src/types/index.ts contains "lengthMiles: number"
    - src/types/index.ts contains "speedBucket: number"
    - src/types/index.ts contains "clusterCount: number"
    - src/types/index.ts does NOT contain "LatLng"
    - src/types/index.ts does NOT contain "Module"
    - npx tsc --noEmit produces zero errors (may have warnings about unused files — that is OK)
  </acceptance_criteria>
  <done>All config files rewritten for Tailwind v3 + Vite 5 + react-leaflet v4. Canonical TypeScript interfaces defined. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx tsc --noEmit` passes
3. package.json shows all pinned versions matching STACK.md
4. tailwind.config.js exists with all design tokens
5. No references to @tailwindcss/vite anywhere in config files
6. src/main.tsx imports leaflet CSS before everything else
</verification>

<success_criteria>
- All packages at correct pinned versions (React 18, react-leaflet 4, Tailwind 3, Vite 5, Recharts 2)
- Tailwind v3 PostCSS pipeline active with design tokens
- TypeScript compiles with zero errors
- Leaflet CSS import is first in main.tsx
- All canonical interfaces defined in types/index.ts with [number, number] tuples
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
