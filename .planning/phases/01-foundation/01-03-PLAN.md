---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/App.tsx
  - src/components/layout/NavBar.tsx
  - src/components/mission-control/MissionControlView.tsx
  - src/components/signal-analytics/SignalAnalyticsView.tsx
autonomous: false
requirements:
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - "Nav bar renders at 48px height with #0d1f3c background"
    - "INRIX IQ logo shows INRIX in bold + IQ in bordered box"
    - "Three right-side icons render: HelpCircle, RefreshCw, LayoutGrid"
    - "Clicking module name text toggles between Mission Control and Signal Analytics views"
    - "Mission Control view shows dark navy (#0a1628) background"
    - "App has no sidebar — only top nav bar"
  artifacts:
    - path: "src/components/layout/NavBar.tsx"
      provides: "Top navigation bar"
      exports: ["NavBar"]
      min_lines: 30
    - path: "src/App.tsx"
      provides: "Root component with view routing"
      contains: "useStore"
    - path: "src/components/mission-control/MissionControlView.tsx"
      provides: "Mission Control placeholder view"
      exports: ["MissionControlView"]
    - path: "src/components/signal-analytics/SignalAnalyticsView.tsx"
      provides: "Signal Analytics placeholder view"
      exports: ["SignalAnalyticsView"]
  key_links:
    - from: "src/App.tsx"
      to: "src/store/index.ts"
      via: "useStore(s => s.activeView)"
      pattern: "useStore.*activeView"
    - from: "src/components/layout/NavBar.tsx"
      to: "src/store/index.ts"
      via: "useStore for setActiveView"
      pattern: "setActiveView"
    - from: "src/App.tsx"
      to: "src/components/layout/NavBar.tsx"
      via: "JSX composition"
      pattern: "<NavBar"
---

<objective>
Build the INRIX IQ app shell: NavBar component matching screenshots exactly, App.tsx with view routing via Zustand, and placeholder views for Mission Control and Signal Analytics. Remove Sidebar-based navigation.

Purpose: This delivers FOUND-03 (dark app shell with nav bar) and FOUND-04 (view switching). The app will render a complete dark shell with working navigation that Phase 2+ will fill with map and chart content.

Output: Working app shell with nav bar, view toggle, and two placeholder views. No sidebar.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/ROADMAP.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-CONTEXT.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 types and Plan 02 store -->
From src/types/index.ts:
  export type ActiveView = 'mission-control' | 'signal-analytics'

From src/store/index.ts:
  export const useStore = create<StoreState>()(...)
  // StoreState includes:
  //   activeView: ActiveView
  //   setActiveView: (view: ActiveView) => void
  //   selectedItem: SelectedItem | null
  //   showTraffic, showAlerts, showCameras: boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NavBar, App.tsx, and placeholder views</name>
  <files>
    src/components/layout/NavBar.tsx
    src/components/mission-control/MissionControlView.tsx
    src/components/signal-analytics/SignalAnalyticsView.tsx
    src/App.tsx
  </files>
  <read_first>
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/App.tsx
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/components/layout/Header.tsx
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/components/layout/Sidebar.tsx
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/store/index.ts
    C:/Users/FisherDoan/Workspaces/inrix-demo/src/types/index.ts
    C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
Create/rewrite 4 files. The old Header.tsx, Sidebar.tsx, and old module components (MissionControl, SignalAnalytics in components/modules/) will remain on disk but will no longer be imported by anything.

**src/components/layout/NavBar.tsx** — EXACT match to CONTEXT.md nav bar spec:

```tsx
import { ChevronRight, HelpCircle, RefreshCw, LayoutGrid } from 'lucide-react'
import { useStore } from '../../store'

const VIEW_LABELS = {
  'mission-control': 'Mission Control',
  'signal-analytics': 'Signal Analytics',
} as const

export function NavBar() {
  const activeView = useStore((s) => s.activeView)
  const setActiveView = useStore((s) => s.setActiveView)

  const toggleView = () => {
    setActiveView(
      activeView === 'mission-control' ? 'signal-analytics' : 'mission-control'
    )
  }

  return (
    <nav
      style={{ background: '#0d1f3c', height: 48, minHeight: 48 }}
      className="flex items-center px-3 border-b border-[#1e3a5f] z-[900] select-none"
    >
      {/* Collapse chevron */}
      <button className="text-[#4a6080] hover:text-white p-1 mr-2">
        <ChevronRight size={14} />
      </button>

      {/* INRIX IQ Logo */}
      <div className="flex items-center mr-3">
        <span className="text-white font-bold text-sm tracking-wide">INRIX</span>
        <span className="border border-white text-white text-[10px] px-0.5 ml-0.5 leading-tight font-normal">IQ</span>
      </div>

      {/* Module name — clickable toggle */}
      <button
        onClick={toggleView}
        className="text-white text-sm font-normal hover:text-[#8ca0bc] transition-colors"
      >
        {VIEW_LABELS[activeView]}
      </button>

      {/* Spacer */}
      <div className="flex-1" />

      {/* Right icons — exactly 3 */}
      <div className="flex items-center gap-2">
        <button className="text-[#4a6080] hover:text-white p-1">
          <HelpCircle size={16} strokeWidth={1.5} />
        </button>
        <button className="text-[#4a6080] hover:text-white p-1">
          <RefreshCw size={16} strokeWidth={1.5} />
        </button>
        <button className="text-[#4a6080] hover:text-white p-1">
          <LayoutGrid size={16} strokeWidth={1.5} />
        </button>
      </div>
    </nav>
  )
}
```

Key requirements from CONTEXT.md:
- Height ~48px, bg #0d1f3c
- Far left: collapse chevron (ChevronRight)
- Logo: "INRIX" bold + "IQ" in bordered box (`border border-white px-0.5 ml-0.5 text-xs font-normal`)
- Center-left: module name, clickable toggles between views
- Right: exactly 3 icons — HelpCircle, RefreshCw, LayoutGrid
- No user avatar, no bell, no other icons

**src/components/mission-control/MissionControlView.tsx** — Phase 1 placeholder:

Create the directory `src/components/mission-control/` if it doesn't exist.

```tsx
export function MissionControlView() {
  return (
    <div className="flex-1 flex flex-col overflow-hidden" style={{ background: '#0a1628' }}>
      {/* Filter bar placeholder — styled, functionality in Phase 2 */}
      <div
        style={{ background: '#0d1f3c', height: 40, minHeight: 40 }}
        className="flex items-center px-4 border-b border-[#1e3a5f] gap-6"
      >
        <span className="text-[#8ca0bc] text-xs">Network</span>
        <span className="text-[#4a6080] text-xs">Corridors</span>
        <span className="text-[#4a6080] text-xs ml-4">Map Version: Latest</span>
        <span className="text-[#8ca0bc] text-xs ml-4">Traffic Flow</span>
        <span className="text-[#8ca0bc] text-xs">Alerts</span>
        <span className="text-[#8ca0bc] text-xs">Cameras</span>
      </div>
      {/* Map placeholder — will be replaced in Phase 2 */}
      <div className="flex-1 flex items-center justify-center" style={{ background: '#0a1628' }}>
        <span className="text-[#4a6080] text-sm select-none">Map renders here in Phase 2</span>
      </div>
    </div>
  )
}
```

**src/components/signal-analytics/SignalAnalyticsView.tsx** — Phase 1 placeholder:

Create the directory `src/components/signal-analytics/` if it doesn't exist.

```tsx
export function SignalAnalyticsView() {
  return (
    <div className="flex-1 flex overflow-hidden" style={{ background: '#e8ecf1' }}>
      {/* Left KPI panel placeholder */}
      <div
        style={{ width: 280, minWidth: 280, background: '#dfe5ed' }}
        className="border-r border-[#c8d0dc] p-4"
      >
        <span className="text-[#4a6080] text-sm">KPI Panel — Phase 4</span>
      </div>
      {/* Center content placeholder */}
      <div className="flex-1 flex items-center justify-center">
        <span className="text-[#4a6080] text-sm select-none">Signal Analytics content renders here in Phase 4</span>
      </div>
    </div>
  )
}
```

**src/App.tsx** — Rewrite completely. Remove Sidebar, ComingSoonModule, old imports. Use new store and new components:

```tsx
import { NavBar } from './components/layout/NavBar'
import { MissionControlView } from './components/mission-control/MissionControlView'
import { SignalAnalyticsView } from './components/signal-analytics/SignalAnalyticsView'
import { useStore } from './store'

function App() {
  const activeView = useStore((s) => s.activeView)

  return (
    <div
      style={{
        width: '100vw',
        height: '100vh',
        display: 'flex',
        flexDirection: 'column',
        overflow: 'hidden',
        background: '#0a1628',
      }}
    >
      <NavBar />
      {activeView === 'mission-control' ? (
        <MissionControlView />
      ) : (
        <SignalAnalyticsView />
      )}
    </div>
  )
}

export default App
```

Key changes from existing App.tsx:
- No Sidebar import or render
- No ComingSoonModule
- Import from './store' (new useStore) NOT './store/useAppStore'
- Read `activeView` (not `activeModule`) from store
- Only 2 views: MissionControlView and SignalAnalyticsView
- Layout is column flex (nav on top, view below) — no row flex with sidebar

After creating these files, verify `npm run build` passes. If there are TS errors from old files that still import the old types (e.g., components/modules/MissionControl.tsx), those files are no longer imported by App.tsx and can be left as-is. The build should still pass because unused files are not included in the bundle. However, `tsc --noEmit` checks ALL files. If tsc errors come from old files, delete or rename the offending old files:
- Delete src/components/modules/ directory (old module components)
- Delete src/components/layout/Sidebar.tsx
- Delete src/components/layout/Header.tsx
- Delete src/store/useAppStore.ts
- Optionally delete src/data/mockSegments.ts, src/data/mockAlerts.ts, src/data/mockCameras.ts (or leave if tsc doesn't error on them)
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx does NOT contain "Sidebar" or "useAppStore" or "activeModule"
    - src/App.tsx contains "import { useStore } from './store'"
    - src/App.tsx contains "useStore((s) => s.activeView)"
    - src/App.tsx contains "<NavBar />"
    - src/App.tsx contains "<MissionControlView />" and "<SignalAnalyticsView />"
    - src/components/layout/NavBar.tsx contains "INRIX" AND "IQ"
    - src/components/layout/NavBar.tsx contains "border border-white" (logo IQ box)
    - src/components/layout/NavBar.tsx contains "HelpCircle" AND "RefreshCw" AND "LayoutGrid"
    - src/components/layout/NavBar.tsx contains "toggleView" or equivalent click handler
    - src/components/layout/NavBar.tsx contains "height: 48" or "height: '48px'" or similar
    - src/components/mission-control/MissionControlView.tsx contains "#0a1628"
    - src/components/signal-analytics/SignalAnalyticsView.tsx exports SignalAnalyticsView
    - npm run build completes with zero errors
    - No Sidebar.tsx is imported anywhere in the final component tree
  </acceptance_criteria>
  <done>App shell renders with NavBar (48px, #0d1f3c, INRIX IQ logo, 3 icons), view toggle works, Mission Control shows dark background, Signal Analytics shows light background. No sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of app shell</name>
  <files>src/App.tsx</files>
  <action>
Human visual verification of the app shell. No code changes in this task — it verifies Task 1 output.

Steps to verify:
1. Run: cd C:/Users/FisherDoan/Workspaces/inrix-demo and npm run dev
2. Open browser to http://localhost:5173
3. Verify: Page background is dark navy (#0a1628)
4. Verify: Top nav bar is slightly lighter navy (#0d1f3c), ~48px tall
5. Verify: Far left has a small chevron icon
6. Verify: "INRIX" in bold white + "IQ" in a small bordered rectangle
7. Verify: "Mission Control" text appears after the logo
8. Verify: Right side has exactly 3 icons (?, refresh circle, grid dots)
9. Click "Mission Control" text — it should change to "Signal Analytics"
10. Verify: View below nav changes from dark background to light grey/blue background
11. Click "Signal Analytics" text — it should change back to "Mission Control"
12. Verify: No sidebar visible anywhere
13. Check browser console — should have zero errors

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User visually confirms: dark navy page, nav bar with INRIX IQ logo and 3 icons, view toggle works, no sidebar, no console errors.</done>
</task>

</tasks>

<verification>
1. `npm run dev` opens dark navy page with no console errors
2. Nav bar shows INRIX IQ logo with bordered IQ box
3. Three right icons visible: HelpCircle, RefreshCw, LayoutGrid
4. Clicking module name toggles between Mission Control and Signal Analytics
5. No sidebar component rendered
6. `npm run build` succeeds
</verification>

<success_criteria>
- FOUND-03: App shell renders with dark navy background and correct nav bar (logo, 3 icons, 48px height, #0d1f3c)
- FOUND-04: Clicking module name in nav toggles between Mission Control and Signal Analytics views
- No sidebar in the app — navigation is top nav only
- Both views render their placeholder content without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
