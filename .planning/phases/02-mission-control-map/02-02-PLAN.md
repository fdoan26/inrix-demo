---
phase: 02-mission-control-map
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/mission-control/FilterBar.tsx
  - src/components/mission-control/MissionControlView.tsx
autonomous: false
requirements: [FILTER-01, FILTER-02, FILTER-03, FILTER-04, FILTER-05]

must_haves:
  truths:
    - "Filter bar renders below NavBar with Network/Corridors toggle, Map Version dropdown, Traffic Flow toggle, Alerts toggle, and Cameras toggle"
    - "Traffic Flow toggle shows/hides road segment polylines on the map"
    - "Alerts toggle shows/hides alert pins and shows a count badge with the number of alerts"
    - "Cameras toggle shows/hides camera pins on the map"
    - "Toggle pills are blue when ON, grey when OFF; Alerts pill is orange when ON"
  artifacts:
    - path: "src/components/mission-control/FilterBar.tsx"
      provides: "Full filter bar with 5 toggle controls"
      min_lines: 60
    - path: "src/components/mission-control/MissionControlView.tsx"
      provides: "Complete Mission Control view wiring FilterBar + MapView"
      min_lines: 20
  key_links:
    - from: "src/components/mission-control/FilterBar.tsx"
      to: "src/store/filterSlice.ts"
      via: "useStore selectors for showTraffic/showAlerts/showCameras and toggle actions"
      pattern: "toggleTraffic|toggleAlerts|toggleCameras"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/components/mission-control/MapView.tsx"
      via: "import and render MapView"
      pattern: "import.*MapView.*from"
    - from: "src/components/mission-control/MissionControlView.tsx"
      to: "src/components/mission-control/FilterBar.tsx"
      via: "import and render FilterBar"
      pattern: "import.*FilterBar.*from"
---

<objective>
Build the FilterBar component with all 5 toggle controls and rewire MissionControlView to compose FilterBar + MapView, replacing the Phase 1 placeholder. Also clean up old `src/components/map/` files that are now superseded.

Purpose: Complete the interactive filter controls that show/hide map layers.
Output: Working FilterBar + fully wired MissionControlView.
</objective>

<execution_context>
@C:/Users/FisherDoan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FisherDoan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/ROADMAP.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/02-mission-control-map/02-CONTEXT.md
@C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/02-mission-control-map/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing store -->

From src/store/filterSlice.ts:
```typescript
export interface FilterSlice {
  activeTab: 'network' | 'corridors'
  mapVersion: string
  showTraffic: boolean
  showAlerts: boolean
  showCameras: boolean
  setActiveTab: (tab: 'network' | 'corridors') => void
  setMapVersion: (version: string) => void
  toggleTraffic: () => void
  toggleAlerts: () => void
  toggleCameras: () => void
}
```

From src/store/index.ts:
```typescript
export const useStore = create<StoreState>()(...)
```

From src/data/alerts.ts:
```typescript
export const alerts: Alert[]  // 23 items — use alerts.length for count badge
```

Plan 01 created:
- src/components/mission-control/MapView.tsx (full map with layers and legends)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterBar and rewire MissionControlView</name>
  <files>src/components/mission-control/FilterBar.tsx, src/components/mission-control/MissionControlView.tsx</files>
  <read_first>
    - C:/Users/FisherDoan/Workspaces/inrix-demo/src/components/mission-control/MissionControlView.tsx
    - C:/Users/FisherDoan/Workspaces/inrix-demo/src/components/mission-control/MapView.tsx
    - C:/Users/FisherDoan/Workspaces/inrix-demo/src/store/filterSlice.ts
    - C:/Users/FisherDoan/Workspaces/inrix-demo/src/store/index.ts
    - C:/Users/FisherDoan/Workspaces/inrix-demo/src/data/alerts.ts
    - C:/Users/FisherDoan/Workspaces/inrix-demo/.planning/phases/02-mission-control-map/02-CONTEXT.md
  </read_first>
  <action>
**src/components/mission-control/FilterBar.tsx:**

Create the filter bar that sits below NavBar. Layout is a single row, dark navy background (#0d1f3c), height 40px, with items left-to-right separated by gaps.

Store reads via useStore selectors:
- `activeTab` from `useStore((s) => s.activeTab)`
- `mapVersion` from `useStore((s) => s.mapVersion)`
- `showTraffic` from `useStore((s) => s.showTraffic)`
- `showAlerts` from `useStore((s) => s.showAlerts)`
- `showCameras` from `useStore((s) => s.showCameras)`

Store actions via useStore selectors:
- `setActiveTab` from `useStore((s) => s.setActiveTab)`
- `toggleTraffic` from `useStore((s) => s.toggleTraffic)`
- `toggleAlerts` from `useStore((s) => s.toggleAlerts)`
- `toggleCameras` from `useStore((s) => s.toggleCameras)`

Import `alerts` from `../../data/alerts` to get `alerts.length` for the count badge.

Layout (left to right):
1. **Network/Corridors toggle** — Two text buttons side by side. Active tab gets `text-white` + subtle bottom border or highlight. Inactive gets `text-[#4a6080]`. Clicking calls `setActiveTab('network')` or `setActiveTab('corridors')`.

2. **Map Version** — Text label "Map Version:" followed by the current `mapVersion` value ("Latest") with a down-chevron icon. This is display-only (dropdown opens in a future phase — not functional now, just shows the label).

3. **Traffic Flow toggle** — A pill switch (small rounded rectangle, 36x18px): blue `#2196f3` background when `showTraffic` is true, grey `#4a5568` when false. White circle indicator (14x14px) that shifts left/right. Label "Traffic Flow" in `text-[#8ca0bc]`. Clicking calls `toggleTraffic()`.

4. **Alerts toggle** — Same pill switch pattern but orange `#f57c00` when `showAlerts` is true (per CONTEXT.md screenshot). Grey `#4a5568` when false. Label "Alerts". Count badge: small rounded rectangle with `alerts.length` displayed (e.g., "23") in white text on blue `#2196f3` background, font-size 10px. Clicking calls `toggleAlerts()`.

5. **Cameras toggle** — Same pill switch pattern, blue `#2196f3` when `showCameras` is true, grey `#4a5568` when false. Label "Cameras". Clicking calls `toggleCameras()`.

The pill switch is a reusable inline element (can be extracted to a local helper function `PillSwitch({ active, color, onClick })`).

Container styles: `style={{ background: '#0d1f3c', height: 40, minHeight: 40 }}` with `className="flex items-center px-4 border-b border-[#1e3a5f] gap-6"`.

**src/components/mission-control/MissionControlView.tsx:**

Rewrite to replace the Phase 1 placeholder. New structure:

```tsx
import { FilterBar } from './FilterBar'
import { MapView } from './MapView'

export function MissionControlView() {
  return (
    <div className="flex-1 flex flex-col overflow-hidden" style={{ background: '#0a1628' }}>
      <FilterBar />
      <MapView />
    </div>
  )
}
```

The existing placeholder content (filter bar stub spans + map placeholder div) is fully replaced.

Also delete the old `src/components/map/` directory entirely:
- `src/components/map/TrafficMap.tsx` (superseded by MapView.tsx)
- `src/components/map/SegmentPolylines.tsx` (superseded by layers/SegmentLayer.tsx)
- `src/components/map/AlertMarkers.tsx` (superseded by layers/AlertLayer.tsx)

And delete old mock data files that are no longer imported:
- `src/data/mockSegments.ts`
- `src/data/mockAlerts.ts`
- `src/data/mockCameras.ts`

Verify no remaining imports reference the deleted files by running `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/mission-control/FilterBar.tsx contains `toggleTraffic` and `toggleAlerts` and `toggleCameras`
    - src/components/mission-control/FilterBar.tsx contains `activeTab` and `setActiveTab`
    - src/components/mission-control/FilterBar.tsx contains `alerts.length` (for count badge)
    - src/components/mission-control/FilterBar.tsx contains `#f57c00` (orange for alerts toggle ON state)
    - src/components/mission-control/FilterBar.tsx contains `#2196f3` (blue for traffic/cameras toggle ON state)
    - src/components/mission-control/FilterBar.tsx contains `#4a5568` (grey for toggle OFF state)
    - src/components/mission-control/FilterBar.tsx contains `Network` and `Corridors` text
    - src/components/mission-control/FilterBar.tsx contains `Map Version`
    - src/components/mission-control/MissionControlView.tsx contains `import { FilterBar }` or `import FilterBar`
    - src/components/mission-control/MissionControlView.tsx contains `import { MapView }` or `import MapView`
    - src/components/mission-control/MissionControlView.tsx contains `<FilterBar` and `<MapView`
    - src/components/map/ directory does not exist
    - src/data/mockSegments.ts does not exist
    - src/data/mockAlerts.ts does not exist
    - src/data/mockCameras.ts does not exist
    - npx tsc --noEmit exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>FilterBar renders all 5 controls with store-connected toggles; MissionControlView composes FilterBar + MapView; old map/ directory and mock data files deleted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Mission Control map view</name>
  <files>none</files>
  <action>
Start dev server with `npm run dev`. Open http://localhost:5173 in browser. Verify the complete Mission Control map view renders correctly: dark CartoDB map with LA, colored segment polylines, teardrop alert pins, black circle camera pins, legends at bottom-left, and working filter toggles.
  </action>
  <what-built>Complete Mission Control map view: CartoDB Dark Matter map centered on LA with 25+ colored segment polylines, 20+ teardrop alert pins, 15+ black circle camera pins, congestion and alerts legends at bottom-left, and a functional filter bar with Traffic Flow / Alerts / Cameras toggles.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Verify the dark map loads with Los Angeles visible, no grey tiles
    3. Verify green/yellow/orange/red road segment polylines are visible on the map
    4. Verify colored teardrop alert pins are visible on the map
    5. Verify black circle camera pins with white numbers are visible
    6. Verify the congestion legend overlay is at bottom-left showing color swatches
    7. Verify the alerts legend overlay is at bottom-left (to the right of congestion legend)
    8. Click the Traffic Flow toggle in the filter bar — road segments should hide/show
    9. Click the Alerts toggle — alert pins and alerts legend should hide/show
    10. Click the Cameras toggle — camera pins should hide/show
    11. Verify the Alerts toggle has an orange pill when ON and shows a count badge (23)
    12. Verify the Network/Corridors text toggles highlight on the active tab
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/FisherDoan/Workspaces/inrix-demo && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms dark map tiles load with LA visible
    - Human confirms colored segment polylines are visible
    - Human confirms alert and camera pins render correctly
    - Human confirms filter toggles show/hide layers
  </acceptance_criteria>
  <done>Human confirms all map layers, legends, and filter bar toggles render and function correctly</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- FilterBar toggles correctly show/hide each map layer
- All 5 filter controls are rendered with correct styling
- Old `src/components/map/` directory and `mockSegments/mockAlerts/mockCameras` files removed
- Human verifies visual correctness of all map layers, legends, and filter interactions
</verification>

<success_criteria>
- FilterBar renders Network/Corridors toggle, Map Version label, Traffic Flow/Alerts/Cameras toggles
- Each toggle reads from and writes to Zustand filterSlice
- Alerts toggle shows orange pill when ON + count badge showing "23"
- Clicking any toggle hides/shows the corresponding map layer
- MissionControlView composes FilterBar + MapView in a flex column
- Old map components and mock data files cleaned up
- Human-verified: all layers, legends, and toggles work visually
</success_criteria>

<output>
After completion, create `.planning/phases/02-mission-control-map/02-02-SUMMARY.md`
</output>
